// Tailwind config is now handled via CSS variables in src/index.css for v4 compatibility
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
